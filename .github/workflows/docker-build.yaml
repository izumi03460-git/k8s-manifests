name: Docker Build

on:
  push:
    branches:
      - main

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Login DockerHub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.izumihidetoshi }}
        password: ${{ secrets.dckr_pat_UpuDxckaMYocnnb1x_7p7XSrQKc }}

    - name: Build Image
      run: |
        docker build -t izumihidetoshi/apache-demo:latest .

    - name: Push Image
      run: |
        docker push izumihidetoshi/apache-demo:latest
